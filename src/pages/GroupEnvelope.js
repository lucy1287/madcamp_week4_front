import React from 'react';
import '../GroupEnvelope.css'; // 올바른 경로로 CSS 파일을 import합니다.

const GroupEnvelope = () => {
    return (
        <div className="group-envelope-page">
            <h1>Group Envelope</h1>
            {/* 여기에 필요한 추가 콘텐츠를 작성할 수 있습니다. */}
        </div>
    );
};

export default GroupEnvelope;
